# Звуковые эффекты для Couch Guardian

## 📁 Структура звуков:

### Основные звуки:
1. **push-positive.mp3** - Положительный толчок (+ очки)
2. **push-negative.mp3** - Отрицательный толчок (- очки)
3. **cursed.mp3** - Проклятый толчок (13-й)
4. **level-up.mp3** - Повышение уровня абсурда
5. **game-start.mp3** - Начало игры
6. **game-over.mp3** - Конец игры
7. **button-click.mp3** - Клик по кнопке
8. **notification.mp3** - Уведомление

### Фоновые звуки:
9. **ambient.mp3** - Фоновая атмосфера
10. **music-loop.mp3** - Фоновая музыка (опционально)

## 🎵 Создание звуков:

### Онлайн генераторы:
1. **BFXR** (https://www.bfxr.net/) - 8-битные звуки
2. **ChipTone** (https://sfxr.me/) - Ретро звуки
3. **JSFXR** (https://github.com/mneubrand/jsfxr) - Онлайн версия

### Бесплатные ресурсы:
- **Freesound.org** (требуется атрибуция)
- **OpenGameArt.org** 
- **Kenney.nl** (бесплатные ассеты)

## 🔧 Интеграция:

### HTML:
```html
<audio id="sound-push-positive" preload="auto">
    <source src="sounds/push-positive.mp3" type="audio/mpeg">
</audio>
```

### JavaScript:
```javascript
const audio = new Audio('sounds/push-positive.mp3');
audio.volume = 0.5;
audio.play();
```

## ⚙️ Настройки по умолчанию:

```javascript
const SOUND_CONFIG = {
    enabled: true,
    volume: 0.5,
    sounds: {
        pushPositive: { volume: 0.6, rate: 1.0 },
        pushNegative: { volume: 0.7, rate: 0.8 },
        cursed: { volume: 0.8, rate: 0.7 },
        levelUp: { volume: 0.6, rate: 1.2 }
    }
};
```

## 🎮 Использование в игре:

### При толчке:
```javascript
function playPushSound(pointsChange) {
    if (pointsChange > 0) {
        playSound('push-positive');
    } else if (pointsChange < -100) {
        playSound('cursed');
    } else {
        playSound('push-negative');
    }
}
```

### При событиях:
```javascript
// Начало игры
playSound('game-start');

// Повышение уровня
playSound('level-up');

// Уведомление
playSound('notification');
```

## 📱 Оптимизация для мобильных:

1. **Предзагрузка** звуков при старте
2. **Web Audio API** для лучшей производительности
3. **Autoplay policy** - воспроизводить только после взаимодействия
4. **Volume control** - настройка громкости в настройках игры

## 🔒 Авторские права:

Убедитесь что все звуки:
1. Либо созданы вами
2. Либо имеют соответствующую лицензию
3. Либо указана атрибуция если требуется

## 🚀 Быстрый старт:

1. Создайте папку `sounds/`
2. Сгенерируйте звуки через BFXR
3. Добавьте в игру через `SoundSystem`
4. Протестируйте на разных устройствах